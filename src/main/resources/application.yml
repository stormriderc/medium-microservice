server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mediumdb?useSSL=false&serverTimezone=UTC
    username: medium-ms
    password: medium-ms
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL57InnoDBDialect
    show-sql: true
    hibernate:
      ddl-auto: update